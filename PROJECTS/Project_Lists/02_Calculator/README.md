# 02 - Calculator Program 🧮

## 📌 Description
This is a simple Python calculator program that performs **addition** and **subtraction** based on the operator entered by the user.

## 🎯 Concepts Used
- User input using `input()`
- Type conversion using `float()`
- Conditional statements (`if`, `elif`)
- Basic arithmetic operations

## ▶️ How to Run
```bash
python main.py
