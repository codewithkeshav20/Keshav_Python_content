# Countdown Timer Program

A countdown timer that counts down from a given number of seconds.

## Features
- Displays time in `HH:MM:SS` format
- Updates every second using `time.sleep(1)`
- Ends with a final message

## Concepts Used
- `for` loop (reverse)
- `time` module
- Formatting with `:02`
