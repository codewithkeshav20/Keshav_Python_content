# Compound Interest Calculator

A compound interest calculator that computes the final balance based on principal, rate, and time.

## Features
- Validates values so they are not negative
- Calculates compound interest using `pow()`
- Displays formatted final result

## Concepts Used
- `while` loops
- Input validation
- `pow()`
- f-string formatting
