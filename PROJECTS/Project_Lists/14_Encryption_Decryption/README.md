# Encryption Program

A simple encryption and decryption program using a shuffled character key.

## Features
- Encrypts text by mapping characters to shuffled keys
- Decrypts text using the same key mapping
- Uses punctuation and ASCII letters for encryption set

## Concepts Used
- `string` module
- Lists and indexing
- `random.shuffle()`
- Character substitution
