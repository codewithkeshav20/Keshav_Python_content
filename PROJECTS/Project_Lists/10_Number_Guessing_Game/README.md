# Number Guessing Game

A guessing game where the computer generates a random number and the user keeps guessing until correct.

## Features
- Random number generation
- Range checking (1 to 100)
- Counts total number of guesses

## Concepts Used
- `random` module
- Loops
- Input validation using `.isdigit()`
